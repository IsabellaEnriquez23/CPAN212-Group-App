import { useNavigate } from "react-router-dom"

const EventDetails = () => {

    const navigate = useNavigate()

    return(
        <div>
            <h2>Event Details</h2>
        </div>
    )
}
export default EventDetails